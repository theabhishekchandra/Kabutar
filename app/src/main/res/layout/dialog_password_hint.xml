<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@drawable/dialog_background">

    <!-- Title -->
    <TextView
        android:id="@+id/passwordHintTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Password Hint"
        android:textSize="18sp"
        android:textStyle="bold"
        android:textColor="@color/black"
        android:layout_marginBottom="8dp" />

    <!-- Message -->
    <TextView
        android:id="@+id/passwordHintMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="This password is provided by Google. You need to generate it through the link below:"
        android:textSize="14sp"
        android:textColor="@color/black"
        android:layout_marginBottom="8dp" />

    <!-- Clickable Link -->
    <TextView
        android:id="@+id/linkTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Generate Password: https://myaccount.google.com/apppasswords"
        android:textSize="14sp"
        android:textColor="@color/teal_700"
        android:autoLink="web"
        android:linksClickable="true"
        android:layout_marginBottom="8dp" />
</LinearLayout>
